# Secrets NEVER belong in the repo — they live in GitHub Actions secrets only.
.env
*.env
!.env.example

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# Local scratch / token refresh artifact (must never be committed)
new_token.txt
*.local

# OS
.DS_Store
Thumbs.db
